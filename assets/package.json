{
  "name": "assets",
  "scripts": {
    "build": "bun run build.ts",
    "watch": "bun --watch run build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "dependencies": {
    "bun": "^1.3.11"
  }
}
